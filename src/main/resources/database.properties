# Database Configuration
db.url=jdbc:mysql://localhost:3306/online_cab_system
db.user=root
db.password=1234
db.driver=com.mysql.cj.jdbc.Driver

# Optional Settings
db.pool.size=10  # Maximum connections in the pool (if using pooling)
